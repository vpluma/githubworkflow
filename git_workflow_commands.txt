Use GIT BASH (in Windows) or Terminal (in Linux/Unix)
cd to yourPersonalLocalDirectory/projectDirectory
git init
git config --replace-all user.name "Firstname Lastname"
git config --replace-all user.email "email_username@gmail.com"
git add * -f
git commit -m "first upload comment"
git remote add origin https://github.com/github_username/project_repo_name.git
git pull origin master
git add * -f
git commit -m "added readme file comment"
git push origin master
GitHub username:  vpluma
GitHub password:  will remain blank while you type (for extra security)










